PREREQUISITES
1. Create and activate a virtual environment in your working directory
    $ python -m venv <name>
    $ venv\Scripts\activate
2. Install the the packages in the 'requirements.txt' file, into the environment
    $ pip install -r requirements.txt